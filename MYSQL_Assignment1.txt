                Assignment-1


Q1) 
?	Enter password: *******
show databases;

Q2)
   -> create table countries(
   - country_id int,
   -  country_name varchar(25),
   -  region_id int);

-> desc countries;

Q3)
-> create table jobs(
- job_id int primary key,
- job_title varchar(20) not null,
- min_salary decimal(7,2),
-max_salary decimal(7,2) check 25000);

Q4)
-> create table job_history(
- employee_id int,
- start_date date,
- end_date date,
- job_id int,
- department_id int);

Q5)
-> alter table countries
- modify column country_id int unique;

Q6)
-> create table job_default(
- job_id int,
- job_title varchar(25) default,
- min_salary decimal(7,2) default 8000,
- max_salary decimal(7,2) default null);

Q7)
-> create table department(
- dept_id decimal(4,0) primary key,
- dept_name varchar(30) not null,
- manager_id decimal(6,0) primary key,
- loc_id decimal(4,0),
Composite Primary Key: (dept_id, manager_id) );

Q8)
-> create table employee(
- employee_id int primary key,
- first_name varchar(50),
- last_name varchar(50),
- email varchar(100),
- phone_no varchar(20)
- hire_date date,
- Job_id int,
- salary decimal(10,2)
- commission decimal(10,2)
- manager_id int,
- dept_id int,
Foreign Key(dept_id) References departments(dept_id) );

Q9)
-> create table department_managers(
- dept_id decimal (4,0) not null,
- manager_id decimal (6,0) not null,
- manager_name varchar (50) not null,
- Primary key (dept_id, manager_id) );


Alter Table employees
Add constraint fk_dept_manager
Foreign key (dept_id, manager_id)
References dept_managers (dept_id, manager_id);
